import { Movie } from "../../models/movie.entity";
import { Role } from "../../models/role.entity";
import { User } from "../../models/user.entity";

const entities = [Role, User, Movie];

export default entities;